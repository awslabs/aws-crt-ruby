{
    "name": "aws-crt-ruby",
    "packages": [],
    "build_steps": [
        "ruby -v",
        "gem install bundler",
        "bundle install",
        "cd gems/aws-crt && bundle exec rake spec"
    ]
}
