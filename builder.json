{
    "name": "aws-crt-ruby",
    "packages": [],
    "build_steps": [
        "ruby -v",
        "gem install bundler",
        "bundle install",
        "bundle exec rake release"
    ],
    "build_env": {
        "CC": "clang",
        "CXX": "clang++"
    }
}
