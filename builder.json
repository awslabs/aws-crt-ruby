{
    "name": "aws-crt-ruby",
    "packages": [],
    "build_steps": [
        "ruby -v",
        "command -v bundler >/dev/null 2>&1 || gem install bundler",
        "bundle install",
        "bundle exec rake release"
    ]
}
