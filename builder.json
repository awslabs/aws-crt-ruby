{
    "name": "aws-crt-ruby",
    "packages": [
        "ruby"
    ],
    "build_steps": [
        "cd aws-crt && gem install bundler",
        "cd aws-crt && bundle install",
        "cd aws-crt && bundle exec rake spec"
    ]
}
