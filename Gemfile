# frozen_string_literal: true

source 'https://rubygems.org'

gem 'aws-eventstream'
gem 'base64'
gem 'ffi'
gem 'rake'

group :test do
  gem 'rspec'
end

group :development do
  gem 'rubocop', require: false
end
